import java.io.*;
class FWriter{
	private BufferedWriter bw;
	FWriter(String s) throws IOException{
		bw = new BufferedWriter(new FileWriter("WrongCode.txt"));
		System.out.println(s);
		bw.write(s);
		bw.close();
	}
}
